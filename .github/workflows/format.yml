name: Formatter

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Black for backend
        uses: psf/black@stable
        with:
          options: '--check --verbose'
          src: './backend'
