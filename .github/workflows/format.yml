name: Formatter

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Black format
        uses: psf/black@stable
        with:
          options: '--check --verbose'
          src: './convokit'
